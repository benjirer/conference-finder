fastapi==0.115.5
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
pyyaml==6.0.2
httpx==0.28.1
python-dateutil==2.9.0
pydantic==2.10.3
anthropic==0.40.0
selectolax==0.3.27

# Test deps — installed via `pip install -r requirements.txt` so tests run on
# Render too. Keeping pytest in the main requirements is fine for a small app.
pytest==8.3.4

pypdf==6.18.0
playwright==1.62.0
python-dotenv==1.2.2
